<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Emoji Display</title>
    <style>
        /* Style to increase the size of the emojis */
        .emoji {
            font-size: 2em; /* Adjust the font size as needed */
        }
    </style>
</head>
<body>
    <h1>Hello</h1>
    <div>
        <p>The emoji for <%= response.name %> is:</p>
        <% if (response.unicode && response.unicode.length > 0) { %>
            <% response.unicode.forEach(code => { %>
                <span class="emoji"><%= String.fromCodePoint(parseInt(code.substring(2), 16)) %></span><!-- Render actual emoji with increased size -->
            <% }); %>
        <% } else { %>
            <p>No emoji available</p>
        <% } %>
    </div>
</body>
</html>

